import React from 'react'
import { useSelector } from 'react-redux'

const Results = () => {
    const afficher = useSelector(state =>state.myproduct); 
  return (
    <div>
     
    </div>
  )
}

export default Results